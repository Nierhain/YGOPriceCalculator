export enum Roles {
    User = "User",
    Mod = "Mod",
    Admin = "Admin",
}

export const RoleLabels: Record<Roles, string> = {
    User: "User",
    Mod: "Moderator",
    Admin: "Administrator",
};
